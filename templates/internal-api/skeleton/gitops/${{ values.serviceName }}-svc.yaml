apiVersion: v1
kind: Service
metadata:
  name: ${{ values.serviceName }}
  namespace: default
spec:
  ports:
    - port: ${{ values.port }}
      protocol: TCP
      targetPort: ${{ values.port }}
  selector:
    app: ${{ values.serviceName }}
  type: ClusterIP
